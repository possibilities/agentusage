[project]
name = "agentuse"
version = "0.1.0"
description = "Poll Claude and Codex usage per account and write structured JSON state"
requires-python = ">=3.11"
dependencies = [
    "pexpect>=4.9",
    "pyte>=0.8.2",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
